# Core Dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
Pillow>=9.0.0
dnspython>=2.2.0
urllib3>=1.26.0

# Network Penetration Testing Dependencies
python-nmap>=0.7.1
scapy>=2.5.0
python-gvm>=21.0.0
ipaddress>=1.0.23
netifaces>=0.11.0

# Web Application Security Testing Dependencies
PyJWT>=2.8.0          # JWT token verification and generation
cryptography>=41.0.0  # Advanced crypto operations
pycryptodome>=3.18.0  # Additional crypto operations
lxml>=4.9.0           # XML/HTML parsing

# OSINT Dependencies
phonenumbers>=8.13.0  # Phone number intelligence
whois>=0.9.0          # WHOIS lookups
shodan>=1.28.0        # Shodan API (requires API key)
python-whois>=0.8.0   # Alternative WHOIS library

# Wireless Security Dependencies (Linux-specific)
# Note: These require system packages on Linux
# - aircrack-ng suite (airmon-ng, airodump-ng, aireplay-ng, aircrack-ng)
# - hostapd, dnsmasq (for Evil Twin)
# - reaver, bully, wash (for WPS attacks)
# - hcitool, bluetoothctl (for Bluetooth)
# - libnfc-bin, mfoc, mfcuk (for RFID/NFC)
# - killerbee (for Zigbee - requires specific hardware)
pyserial>=3.5         # Serial communication for RF devices

# Mobile Security Dependencies
frida>=16.0.0         # Dynamic instrumentation framework
frida-tools>=12.0.0   # Frida command-line tools
plistlib>=0.6.0       # iOS plist parsing (included in Python 3.4+)

# Digital Forensics Dependencies
# Note: Volatility requires separate installation
# - volatility (for memory forensics - install separately based on OS)
Pillow>=9.0.0         # Image analysis for steganography detection

# Password Cracking & Cryptography Dependencies
# Note: External tools required:
# - hashcat (GPU-accelerated password cracking)
# - john (John the Ripper password cracker)
# - hydra (online brute-force tool)
# Install via package manager: apt-get install hashcat john hydra
pyotp>=2.9.0          # TOTP/HOTP generation for 2FA testing
qrcode>=7.4.0         # QR code generation
pyzbar>=0.1.9         # QR code decoding (requires system libzbar)
scipy>=1.10.0         # Statistical tests for entropy analysis

# Exploitation & Post-Exploitation Dependencies
# Note: External tools required:
# - Metasploit Framework (msfconsole, msfvenom)
# - Impacket tools (psexec.py, wmiexec.py, smbexec.py, dcomexec.py)
# Install Metasploit: https://www.metasploit.com/download
# Install Impacket: pip install impacket
boto3>=1.26.0         # AWS IAM auditing
azure-identity>=1.12.0 # Azure authentication
azure-mgmt-authorization>=3.0.0 # Azure RBAC management
azure-mgmt-resource>=22.0.0 # Azure subscription management

# Data Processing & Analysis
pandas>=1.5.0         # Data analysis (optional but recommended)
matplotlib>=3.6.0     # Visualization (optional)

